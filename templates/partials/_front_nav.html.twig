<nav class="navbar navbar-expand-lg bg-dark text-uppercase fixed-top p-0" id="mainNav" data-bs-theme="dark">
    <a class="navbar-brand pt-4" href="{{ path('app_home') }}">No Sushis</a>
    <div class="navbar-bg-wrapper">
        <div class="navbar-bg bg-1 active">
            <span class="title-page">Tous</span>
        </div>
        <div class="navbar-bg bg-2 translateY-100">
            <span class="title-page send-up"></span>
        </div>
    </div>
    <div class="container pt-4">
        <button class="navbar-toggler text-uppercase font-weight-bold bg-dark text-white rounded ms-auto" type="button" data-bs-toggle="collapse" data-bs-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            Menu
            <i class="fas fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item mx-0 mx-lg-1"><a class="nav-link py-3 px-0 px-lg-3 rounded" href="{{ path('app_home') }}">Home</a></li>

                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item dropdown">
                        <a class="nav-link py-3 px-0 px-lg-3 rounded dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Admin
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="{{ path('app_category_index') }}">Catégories</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="{{ path('app_dish_index') }}">Produits</a></li>
                        </ul>
                    </li>
                {% endif %}

                {% if app.user %}
                    <li class="nav-item mx-0 mx-lg-1"><a class="nav-link py-3 px-0 px-lg-3 rounded" href="{{ path('app_logout') }}"><i class="fa-solid fa-arrow-right-to-bracket pe-2"></i>Logout</a></li>
                {% else %}
                    <li class="nav-item mx-0 mx-lg-1"><a class="nav-link py-3 px-0 px-lg-3 rounded" href="{{ path('app_login') }}"><i class="fa-solid fa-bolt-lightning pe-2"></i>Login</a></li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
